npm install redux react-redux redux-thunk

1. Action

   ex: { type: "ACTION_TYPE", payload:data }

2. Reducer

   const initState = {
        userlist:[],
        productList:[]
   };

   function UserReudcer(state=initState, action) {

        if(action.type == 'ADD_USER') {

            //save state/payload in redux store

        }
        else if(action.type == 'ADD_PRODUCT') {

            //save state/payload in redux store
        }

       return state; 
   }


3.  Redux configuration methods:
   
   => createStore( Reducer )
   => combineStores( All Reducers )
   => connect( ComponentName )
   => <Provider> <React Components> </Provider>
   => mapStateToProps
   => dispatch(Action)




